<?php

/**
 * Implements hook_install().
 *
 * Perform actions to set up the site for this profile.
 */
function quality_install() {
  $default_theme = 'bootstrap';
  variable_set('theme_default', $default_theme);
}
